# Exported Site: glanove.framer.website
Exported on 2026-05-20 by NoCodeExport

## How to View Your Site
This export uses **Maximum Fidelity Mode** — Framer's actual runtime is mirrored locally for pixel-perfect rendering. Some secondary fetches (CMS data, dynamic font loaders) still reach `framerusercontent.com` at runtime, so an active internet connection improves loading. **You cannot open `index.html` directly in a browser** — it must be served over HTTP. See [RUN_LOCALLY.md](RUN_LOCALLY.md).

### Quick Start
```bash
cd glanove-framer-website-export   # or wherever you extracted this
npx serve
# open http://localhost:3000
```

For the why-and-how, see [RUN_LOCALLY.md](RUN_LOCALLY.md).


## Maximum Fidelity Mode

Framer runtime mirrored locally: **292 chunks, 92844 KB**.

> ⚠️ **Re-export when you republish in Framer.** Framer rotates internal asset hashes on every publish. This snapshot is frozen — re-export through NoCodeExport within ~2 weeks to refresh the runtime chunks before old hashes expire.

**Detailed run/deploy instructions: [RUN_LOCALLY.md](RUN_LOCALLY.md)**

## Deployment
Deploy to any static host:

- **Netlify** — drag & drop this folder to [netlify.com/drop](https://app.netlify.com/drop)
- **Vercel** — `vercel deploy`
- **Cloudflare Pages** — push to a repo and connect
- **GitHub Pages** — push to a repo and enable Pages
- **Your own server** — upload via SCP/SFTP, point web root at this folder

> All these hosts serve over HTTPS by default, which is required for the runtime mirror. See [RUN_LOCALLY.md](RUN_LOCALLY.md) for details on deploying behind your own server with the right CORS / MIME headers.

## Platform Detected
framer (100% confidence)

## Export Stats
- Pages: 15

## Notes
- Framer's runtime chunks are mirrored locally; structural content renders without internet
- Re-export required when source republishes (Framer rotates chunk hashes)
- Some metadata or platform-specific references may still point to the source site
- Forms may need backend configuration (see HTML comments near form elements)
- Generated by NoCodeExport (nocodeexport.com)

## Need More Than a Static Export?

NoCodeExport offers a professional Next.js rebuild service — your site hand-coded in Next.js with perfect SEO, blazing performance, and full customization.

Learn more: https://www.nocodeexport.com/en/nextjs-rebuild
